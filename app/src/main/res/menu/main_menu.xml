<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Start/stop the BLE bridge from the action bar. Icon + title are updated
         in onPrepareOptionsMenu based on the current searching state. -->
    <item
        android:id="@+id/action_toggle_bridge"
        android:title="@string/start_service"
        app:actionLayout="@layout/action_toggle"
        app:showAsAction="always" />
</menu>
